ESCUDO V1
=========
NO borra PostgreSQL. NO cambia DATABASE_URL. NO cambia tablas.
NO cambia histórico, algoritmos, Analizar ni recolector automático.

ANTES DEL DEPLOY:
Render > Environment > Add Environment Variable
ADMIN_API_KEY = una clave larga y privada (32+ caracteres).

Protege operaciones manuales:
sync-now, history-step, freeze-next, auto-freeze-now, import-csv, draw/manual.

Los procesos automáticos internos siguen funcionando.

Después del deploy:
 /health
 /api/security/status
 /api/network/history-status
